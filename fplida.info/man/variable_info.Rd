% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable_info.R
\name{variable_info}
\alias{variable_info}
\title{Get variable information}
\usage{
variable_info(
  dataset = NULL,
  asset = NULL,
  topic = NULL,
  collection_type = NULL,
  record_type = NULL,
  value_support_status = NULL
)
}
\arguments{
\item{dataset}{Optional character vector of dataset codes. The match is not
case-sensitive.}

\item{asset}{Optional character vector of asset codes. The match is not
case-sensitive.}

\item{topic}{Optional character vector of topic tags. Each value must match
one complete comma-separated tag. The function does not use partial
matches.}

\item{collection_type}{Optional character vector of collection types.
The match is not case-sensitive.}

\item{record_type}{Optional character vector of record types. The match is
not case-sensitive.}

\item{value_support_status}{Optional character vector of value-support
statuses. The match is not case-sensitive.}
}
\value{
A data frame with one row for each selected variable occurrence.
}
\description{
\code{variable_info()} returns the central variable registry for PLIDA and
BLADE. Each row records one occurrence of a variable or BLADE linking key
in the official metadata. A variable can occur in more than one product or
table.
}
\details{
The registry gives descriptions, source details, periods, value information,
support status and topic tags. The \code{valid_values} variable contains a JSON
array of display entries for normalised value sets that are available in
the bundled public source material. Entries are raw values or
\code{value: label} strings. Use the linked source before treating an entry as a
machine code. The raw BLADE response text remains in
\code{official_valid_response} when no normalised array is available.
\code{variable_type} and \code{variable_level} are blank when the source metadata does
not supply them.
}
\section{Value support}{

\code{value_support_status} records where a variable's value domain came from.

\code{sourced} means published metadata gives the value domain: the codes in
\code{valid_values} come from a classification or code list, and \code{value_source}
names it.

\code{guessed} means the value domain is inferred from the variable's name and
description, not from a published list. A variable whose name ends \verb{_STATE}
is given the Australian state codes on that basis alone. The generated
column therefore holds plausible values of the right shape, but the source
does not confirm them. Treat the codes as a placeholder, not a mapping.

\code{unsupported} means the value domain is neither published nor inferable, so
the column is written as typed missing rather than given invented codes. The
\code{limitation} variable gives the reason.

Survey variables use \code{not_applicable} because the registry does not assess
survey values.
}

\seealso{
\code{\link[=dataset_info]{dataset_info()}}
}
