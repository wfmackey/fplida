% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/value_tables.R
\name{get_values}
\alias{get_values}
\alias{get_mbs_item_numbers}
\alias{get_pbs_item_codes}
\alias{get_mesh_blocks}
\alias{get_sa1_codes}
\alias{get_sa2_codes}
\alias{get_country_codes}
\title{Get a code list too large to print in the variable registry}
\usage{
get_values(key = NULL)

get_mbs_item_numbers()

get_pbs_item_codes()

get_mesh_blocks()

get_sa1_codes()

get_sa2_codes()

get_country_codes()
}
\arguments{
\item{key}{Character. A table key, as listed by \code{get_values()}.}
}
\value{
A data frame of codes, with \code{code} first and a \code{label} column where
the source publishes names. Called with no argument, the catalogue.
}
\description{
A classification with thousands of codes cannot sit in a \code{variable_info()}
row, so those variables record how many values the source publishes and name
the source. \code{get_values()} returns the list itself for the classifications
the package ships.
}
\details{
Call it with no argument for the catalogue: one row per table, with the key
to ask for, how many codes it holds, and where the codes came from.
}
\examples{
# What can be fetched.
get_values()

# One classification.
head(get_values("sa2"))
}
\seealso{
\code{\link[=variable_values]{variable_values()}} for the code lists small enough to sit in the
registry itself, and \code{\link[=variable_info]{variable_info()}} for the registry.
}
