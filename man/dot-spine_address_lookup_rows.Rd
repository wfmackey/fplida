% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/complete_dil_structures.R
\name{.spine_address_lookup_rows}
\alias{.spine_address_lookup_rows}
\title{Mesh block lookup rows for a person's dwelling}
\usage{
.spine_address_lookup_rows(
  spine_rows,
  agency = NULL,
  seed = 0L,
  reference_year = 2021L
)
}
\arguments{
\item{spine_rows}{data.frame. Spine rows, needing \code{state} and ideally
\code{sa2_code} and \code{dwelling_id}.}
}
\value{
A data.frame of lookup rows, one per spine row, with \code{mb_code},
\code{sa1_code}, \code{sa2_code}, \code{sa4_code} and \code{state}.
}
\description{
The address a product reports for a person is a property of the dwelling
they live in, not of the product or of the month the table was written.
This returns the mesh block lookup row for each spine row, restricted to
the SA2 the spine assigned and indexed by the dwelling, which is the same
rule \code{project_core_locations__()} and \code{.dil_asgs_2021_value()} apply. Every
product that uses it therefore agrees with CORE Locations and with every
other product, without a central pass or any shared state.
}
\keyword{internal}
