% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_census.R
\name{generate_census}
\alias{generate_census}
\title{Generate simulated 2021 Census data}
\usage{
generate_census(
  spine = NULL,
  seed = 42L,
  output_dir = NULL,
  format = c("parquet", "csv"),
  return_data = TRUE
)
}
\arguments{
\item{spine}{Data.frame (from \code{generate_spine()}) or NULL. If NULL,
the most recent spine RDS is loaded from \code{get_data_path()}.}

\item{seed}{Integer. Random seed for Census-specific column generation.}

\item{output_dir}{Character or NULL. Base output directory. If NULL, uses
\code{get_data_path()}. One of the two must be set.}

\item{format}{Character. Output format: "parquet" (default) or "csv".}

\item{return_data}{Logical. If TRUE (default), return data.frames in memory;
if FALSE, write to disk and return metadata only.}
}
\value{
A named list with three data.frames:
\describe{
\item{person}{Person-level records (census_2021_person)}
\item{family}{Family-level records (census_2021_family)}
\item{dwelling}{Dwelling-level records (census_2021_dwelling)}
}
}
\description{
Projects Census 2021 person, family, and dwelling tables from the fplida
spine. Person-level records are derived from the spine's demographics,
education, occupation, and income attributes; family and dwelling records
are derived from the linked people; and Census-specific columns (marital
status, language, health conditions, etc.) are generated with age- and
occupation-conditioned distributions.
}
\section{Dataset and variable information}{

The \href{https://www.abs.gov.au/statistics/microdata-tablebuilder/available-microdata-tablebuilder/census-population-and-housing}{ABS Census microdata}
website gives information about this dataset. Use \code{dataset_info("CENSUS")}
for dataset information. Use \code{variable_info("CENSUS")} for variables,
sources, value support, and topic tags.
}

\examples{
\dontrun{
spine <- generate_spine(n = 1000L, seed = 1L)
census <- generate_census(spine = spine, seed = 1L)
str(census$person)
}

}
