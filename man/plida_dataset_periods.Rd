% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_years.R
\name{plida_dataset_periods}
\alias{plida_dataset_periods}
\title{Reference periods of every PLIDA dataset}
\usage{
plida_dataset_periods(dataset = NULL)
}
\arguments{
\item{dataset}{Character, or \code{NULL} for every dataset. Named as the registry
writes it (\code{"TVA"}) or as \code{\link[=build_fplida]{build_fplida()}} names the product (\code{"tva"}), the
same as \code{\link[=plida_dataset_years]{plida_dataset_years()}}. A name the registry does not carry gives
no rows, which is the table's way of saying what \code{plida_dataset_years()}
says with \code{NULL}: no declared period, so nothing restricts it.}
}
\value{
A data frame with one row per dataset: \code{dataset}, the
\code{reference_period} text from the registry, the parsed \code{years} in compact
form, \code{first_year}, \code{last_year} and \code{n_years}.
}
\description{
The companion to \code{\link[=plida_dataset_years]{plida_dataset_years()}}: one row per dataset acronym in the
bundled registry, with the period as the metadata writes it and as the
generators read it. Use it to see why a build dropped a year.
}
\examples{
plida_dataset_periods("TVA")
periods <- plida_dataset_periods()
periods[periods$dataset \%in\% c("TVA", "HE", "PIT_PS"), ]

}
\seealso{
\code{\link[=plida_dataset_years]{plida_dataset_years()}} for one dataset's years.
}
