% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_lfs.R
\name{generate_lfs}
\alias{generate_lfs}
\title{Generate Longitudinal Labour Force Survey data}
\usage{
generate_lfs(
  spine = NULL,
  seed = 42L,
  output_dir = NULL,
  format = c("parquet", "csv"),
  return_data = TRUE,
  sample_household_rate = 0.01,
  max_households = 30000L,
  survey_year = 2025L
)
}
\arguments{
\item{spine}{Data.frame from \code{generate_spine()} or NULL. If NULL,
the spine is loaded from the active run directory.}

\item{seed}{Integer. Random seed.}

\item{output_dir}{Character or NULL. Base output directory. If NULL,
uses \code{get_data_path()}.}

\item{format}{Character. Currently parquet only. Passing \code{"csv"} is
accepted by the argument matcher but rejected at runtime.}

\item{return_data}{Logical. If TRUE, returns generated data.frames in
addition to writing parquet.}

\item{sample_household_rate}{Numeric. Fraction of eligible households
selected into the LLFS panel before applying \code{max_households}.}

\item{max_households}{Integer. Maximum selected households. Keeps full
builds bounded while preserving household-level sampling.}

\item{survey_year}{Integer. Calendar year for household entry cohorts.
Each selected household contributes up to eight consecutive monthly
responses from its first selection month, so output can extend into
the following year.}
}
\description{
Projects a household-based, person-month LLFS sample from the fplida
spine using the April 2026 ABS 6602.0 Longitudinal Labour Force data item
list. Selected households are followed for up to eight
monthly responses, with household attrition and workbook-driven
monthly/quarterly collection cadence. The generator also writes the
Characteristics of Employment supplementary person file (\code{pmp-coes}) for
August respondents, using the ABS COE DataLab item list.
}
\section{Dataset and variable information}{

The \href{https://www.abs.gov.au/statistics/microdata-tablebuilder/available-microdata-tablebuilder/longitudinal-labour-force-australia}{ABS Longitudinal Labour Force microdata}
website gives information about this dataset. Use \code{dataset_info("LFS")} for
dataset information. Use \code{variable_info("LFS")} for variables, sources,
value support, and topic tags.
}

