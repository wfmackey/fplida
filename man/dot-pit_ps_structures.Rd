% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_pit_ps.R
\name{.pit_ps_structures}
\alias{.pit_ps_structures}
\title{Every PIT_PS output table the data item list declares}
\usage{
.pit_ps_structures()
}
\value{
A data.frame with one row per output table: \code{product}, \code{table},
\code{stem} (the file name the package gives a product's table), \code{fy},
\code{months} (the extract window), \code{key_var} (the business identifier column
the registry declares, \code{""} for a table that names no business), \code{geo}
(1 for a geography-only table), and \code{variables}, a list column of the
declared variable names in registry order.
}
\description{
Payment summaries are published one to three tables per product: a single
annual table up to 2018-19, and from 2019-20 several extracts of the same
year that differ only in how long after 30 June the file was cut. The four
products from 2015-16 to 2018-19 add a separate geography table. The split
is read from the bundled data item list rather than typed here, so the
emitted tables cannot drift from the registry.
}
\keyword{internal}
