% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_mbs.R
\name{generate_mbs}
\alias{generate_mbs}
\title{Generate MBS dataset (Medicare Benefits Schedule Claims)}
\usage{
generate_mbs(
  spine = NULL,
  seed = 42L,
  years = 2006L:2025L,
  output_dir = NULL,
  format = c("parquet", "csv"),
  return_data = FALSE,
  chunk_size = 100000L
)
}
\arguments{
\item{spine}{Data.frame from \code{generate_spine()} or NULL to load
from the run directory.}

\item{seed}{Integer random seed for MBS generation.}

\item{years}{Integer vector of calendar years to generate.}

\item{output_dir}{Character or NULL. Base output directory.}

\item{format}{Character. "parquet" (default) or "csv".}

\item{return_data}{Logical. If TRUE, reads back the written
parquet files and returns a named list keyed by year. Default
FALSE (stream to disk only, return metadata).}

\item{chunk_size}{Integer. Persons per streaming write batch.
Default 100000. Peak memory per year is bounded by this.}
}
\value{
If \code{return_data = TRUE}: named list keyed by year, each
a data.frame of claims. If FALSE: metadata with per-year counts.
}
\description{
Consolidated single-Rust-call entry point. All heavy work — spine
column access, participant selection, provider/practice pool
generation, year loop, per-year chunked claim generation, and
parquet writes — lives inside \code{generate_mbs_full__}. The R
wrapper is a thin facade that loads the item lookup (cached CSV)
and the spine column vectors, then makes a single Rust call.
}
\details{
Year-level parallelism: \code{generate_mbs_full__} uses \code{rayon::par_iter}
across years, so multiple years are generated concurrently. Each
year uses a streaming ArrowWriter that flushes per-chunk
RecordBatches, bounding memory to \code{chunk_size} persons' worth.
}
\section{Dataset and variable information}{

\href{https://www.mbsonline.gov.au/}{MBS Online} gives information about this
dataset. Use \code{dataset_info("MBS")} for dataset information. Use
\code{variable_info("MBS")} for variables, sources, value support, and topic tags.
}

