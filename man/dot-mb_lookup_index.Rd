% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_core.R
\name{.mb_lookup_index}
\alias{.mb_lookup_index}
\title{Row index of the Mesh Block lookup, grouped by geography (cached)}
\usage{
.mb_lookup_index()
}
\value{
A list of \code{sa2} and \code{state} indexes, each with \code{rows} (lookup row
numbers ordered by group), \code{key} (the group value), \code{offset} (0-based
start of the group in \code{rows}) and \code{size}.
}
\description{
Every address draw picks a mesh block from the pool of lookup rows sharing
one SA2, or one state when the SA2 is unknown. Finding that pool with
\code{which(lookup$sa2_code == sa2)} rescans all 368,000 rows once per group,
which costs seconds per call and dominated STP: it draws addresses once per
month per product, so it paid that scan 140 times in a six-year build.
}
\details{
The pools do not depend on the spine, so they are built once here. Rows are
held in a single vector ordered by group, with each group's offset and size
alongside, so a pool is a contiguous slice rather than a scan. \code{order()} is
stable, so a group's rows come back in ascending row order — exactly what
\code{which()} returned.
}
\keyword{internal}
