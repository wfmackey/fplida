% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_to_csv.R
\name{convert_parquet_dir_to_csv}
\alias{convert_parquet_dir_to_csv}
\title{Convert a parquet fplida run directory to CSV via DuckDB}
\usage{
convert_parquet_dir_to_csv(
  src_dir,
  dst_dir = NULL,
  threads = NULL,
  memory_limit = "4GB",
  log_file = NULL,
  verbose = interactive(),
  preserve_parquet_datasets = character(),
  messy_files = TRUE,
  messy_names = TRUE
)
}
\arguments{
\item{src_dir}{Character. Parquet run directory (e.g. fplida_30m/).}

\item{dst_dir}{Character. Output root directory. Created if missing.
If NULL, uses \code{paste0(src_dir, "_csv")}.}

\item{threads}{Integer. \verb{PRAGMA threads} — DuckDB parallelism. Default
is \code{NULL}, which uses \code{parallel::detectCores()} capped at 10. On a host
where core detection fails, this falls back to 1.}

\item{memory_limit}{Character. \verb{PRAGMA memory_limit}. Default "4GB".}

\item{log_file}{Character or NULL. Path to a plain-text log. NULL
skips logging (default).}

\item{verbose}{Logical. If TRUE, print per-file progress to the
console (default TRUE when interactive, FALSE otherwise).}

\item{preserve_parquet_datasets}{Character vector of top-level dataset
directory names to keep as parquet under \code{dst_dir}, e.g. \code{"ato-stp"}.}

\item{messy_files}{Logical. If TRUE, emit each PLIDA data product as a
top-level \verb{<product>/<product>.csv} folder, preserve STP parquet products
under \verb{ato-stp/stp-standard/<product>/} or
\verb{ato-stp/stp-extended/<product>/}, emit BLADE products under
\verb{abs-blade/<product>/}, and emit agency spines as top-level
\verb{<agency>-spine-v6/<agency>-spine-v6.csv} folders instead of keeping
other agency-dataset folders.}

\item{messy_names}{Logical. If TRUE, vary a small subset of variable identifiers
across related PIT/PAYG, MBS, and PBS year products by using case changes
and close aliases.}
}
\value{
Invisibly, a list with fields:
\describe{
\item{total_files}{Number of parquet inputs converted}
\item{total_rows}{Total row count across all files}
\item{total_bytes}{Total CSV output bytes}
\item{total_elapsed_sec}{Wall-clock time in seconds}
\item{per_file}{data.frame of per-file timings}
}
}
\description{
Walks \code{src_dir} recursively. Every \code{.parquet} file is rewritten as a
sibling \code{.csv} under \code{dst_dir}, preserving the agency-dataset folder
structure, except datasets listed in \code{preserve_parquet_datasets}.
Preserved datasets are linked/copied as parquet. When
\code{messy_files = TRUE}, each PLIDA data product is written to its own
top-level \verb{<product>/} folder, agency spine files are written once under
top-level \verb{<agency>-spine-v6/} folders, and agency-dataset folders are
omitted. BLADE products keep the same product-folder structure under
\verb{abs-blade/}, and STP parquet products keep it under
\verb{ato-stp/stp-standard/} or \verb{ato-stp/stp-extended/}.
Partitioned product directories (containing \code{part-NNN.parquet} files) are
rewritten as a single unified CSV at \verb{<product>/<product>.csv}, or under
the dataset exception folder for BLADE.
}
\details{
DuckDB's \verb{COPY (SELECT * FROM read_parquet(...)) TO 'x.csv' (FORMAT CSV, HEADER)} pipeline was benchmarked on 1 GB 30m fplida census data at
roughly 6x arrow::write_csv_arrow and 85x arrow::read_parquet +
data.table::fwrite. It streams end-to-end in C++ without any R-side
data.frame materialisation. Row totals are read from Parquet row-group
metadata, avoiding a second full data scan after each CSV write.
}
