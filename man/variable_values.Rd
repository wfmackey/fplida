% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable_info.R
\name{variable_values}
\alias{variable_values}
\title{Get the documented values for variables that have them}
\usage{
variable_values(dataset = NULL, variable = NULL, value_support_status = NULL)
}
\arguments{
\item{dataset}{Optional character vector of dataset codes. The match is not
case-sensitive.}

\item{variable}{Optional character vector of variable names. The match is
not case-sensitive.}

\item{value_support_status}{Optional character vector of value-support
statuses. Pass \code{"sourced"} to exclude codes inferred from a variable's
name.}
}
\value{
A data frame with one row for each code, and the columns \code{dataset},
\code{variable}, \code{code}, \code{label}, \code{value_domain}, \code{value_source} and
\code{value_support_status}. \code{label} repeats \code{code} where the source gives a
bare value rather than a \code{code: label} pair.
}
\description{
\code{variable_values()} returns one row per code, rather than one row per
variable occurrence with the codes packed into a JSON string. Most variables
have no code list at all, so filtering \code{variable_info()} on \code{valid_values}
and then parsing the JSON is the wrong shape for the question people
actually ask, which is "what can this column contain?".
}
\details{
Each variable is returned once, not once per occurrence: a code frame that
appears in 300 tables is still one set of codes.
}
\examples{
# Every code the registry documents for one variable.
variable_values("MBS", "BILLTYPECD")

# Which variables in a dataset have a documented code list at all.
unique(variable_values("DOMINO")$variable)
}
\seealso{
\code{\link[=variable_info]{variable_info()}} for the full registry, including the variables
that have no code list.
}
