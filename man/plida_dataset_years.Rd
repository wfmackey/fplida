% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset_years.R
\name{plida_dataset_years}
\alias{plida_dataset_years}
\title{Years a PLIDA dataset covers}
\usage{
plida_dataset_years(dataset)
}
\arguments{
\item{dataset}{Character. One dataset, named either as the registry writes
it (\code{"TVA"}, \code{"PIT_PS"}, \code{"A&T"}) or as \code{\link[=build_fplida]{build_fplida()}} names the product
(\code{"tva"}, \code{"pit_ps"}, \code{"apprentice"}). Case does not matter.}
}
\value{
An integer vector of years, sorted. \code{NULL} when the dataset declares
no period -- an unknown name, the spine, or BLADE, whose periods are per
table. \code{NULL} means unrestricted, never "no years".
}
\description{
Reports the reference period the bundled PLIDA registry declares for a
dataset, as a vector of years. Every year-aware generator narrows its
\code{years} argument to this before generating, so a build never writes a
product year that does not exist in PLIDA.
}
\details{
Years are calendar years for calendar-year datasets and financial-year END
years for financial-year datasets, matching what the generators' \code{years}
arguments already mean. \code{PIT_PS} covering the 2001-02 to 2022-23 financial
years therefore returns \code{2002:2023}.

A period written as "... to current" is closed at 2026, the end of the
newest financial year any bundled metadata declares. The value is fixed, not
read from the system clock, so the same build gives the same answer next
year.
}
\examples{
plida_dataset_years("TVA")
plida_dataset_years("pit_ps")
plida_dataset_years("CENSUS")

}
\seealso{
\code{\link[=plida_dataset_periods]{plida_dataset_periods()}} for every dataset at once.
}
